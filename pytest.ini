[pytest]
DJANGO_SETTINGS_MODULE = core.settings
# Models are managed = False, so there are no migrations — conftest.py loads
# tests/schema.sql into the test database instead. --no-migrations keeps
# pytest-django from trying to migrate it.
addopts = --no-migrations --strict-markers -ra
python_files = test_*.py
testpaths = apps
