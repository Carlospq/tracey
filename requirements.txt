# Runtime dependencies for traceyDB (production + dev share this file).
# Dev-only tools (linters, test tooling) live in requirements-dev.txt.
# Python 3.10 — keep versions pinned so CI and the server match.

# --- Django core ---
Django==4.2.30
asgiref==3.12.1
sqlparse==0.6.0
pytz==2021.3
gunicorn==20.1.0
whitenoise==5.3.0
dj-database-url==3.1.2
mysqlclient==2.2.8

# --- Django apps / middleware ---
django-allauth==0.51.0
django-simple-captcha==0.7.0
django-environ==0.14.0
django-extensions==3.2.3
django-htmx==1.29.0
tablib==3.10.0
django-tables2==2.7.0
django-csp==4.0
django-permissions-policy==4.33.0
Unipath==1.1

# --- HTTP / scraping / graphs (used by management commands and plots.py) ---
requests==2.34.2
beautifulsoup4==4.15.0
networkx==3.6.1
plotly==5.24.1

# --- Bioinformatics / scientific stack ---
pyhmmer==0.10.15
biopython==1.83
dna-features-viewer==3.1.5
matplotlib==3.11.1
pandas==2.2.0
numpy==1.26.4
scikit-learn==1.2.2  # loads pickled lm_*.sav models — must match the version that created them
xmltodict==1.0.4
